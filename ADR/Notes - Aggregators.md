# High Level Notes
- In theory, if you stored an Aggregator in a TableBuilder, and stored the TableBuilder in the Artefact pipeline, you could effectively have stateful table generation on the fly.

# Future Opportunities
- Add a Basic list aggregator with before functions.